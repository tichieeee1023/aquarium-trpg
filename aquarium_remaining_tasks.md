# 작업 목적

이 작업의 목적은 단순한 버그 수정이나 기능 추가가 아니다.

《아쿠아리움: 심해의 균열》은 각 스테이지가 독립된 미니게임처럼 이어지는 구조가 아니라,

**한 번의 사고가 연쇄적으로 커지면서 플레이어를 점점 더 위험한 공간으로 몰아넣는 짧은 탈출형 TRPG**

를 목표로 한다.

따라서 이번 수정의 가장 중요한 목표는 초기에 설계했던 **시나리오의 인과관계와 연출 흐름을 되살리는 것**이다.

---

## 1. 시나리오의 흐름 복구

각 스테이지는 단순히

```text
조사
→ 주사위
→ 다음 스테이지
```

로 넘어가서는 안 된다.

플레이어가 다음 상황을 자연스럽게 이해할 수 있도록

```text
장면 진입
→ 탐색
→ 위험 징후 발견
→ AP 소진
→ 사건 발생
→ 직업/장비를 이용한 위기 돌파
→ 사건의 결과가 다음 스테이지로 연결
```

되는 흐름을 유지한다.

예를 들어 STAGE 1의 누전 폭발은 단순한 효과가 아니라,
이후 아쿠아리움 전체의 상황이 급격히 악화되었다는 것을 체감시키는 전환점이어야 한다.

---

## 2. 플레이어가 직접 선택했다는 감각 유지

탐사 카드 6개 중 플레이어가 실제로 조사할 수 있는 것은 일부뿐이다.

따라서

- 어떤 장소를 조사했는지
- 어떤 장비를 얻었는지
- 어떤 위험을 미리 발견했는지
- 어떤 직업을 선택했는지

가 이후 생존 방식에 영향을 주어야 한다.

좋은 장비를 전부 모으는 게임이 아니라,

**제한된 행동력 안에서 무엇을 조사했는지가 결과를 바꾸는 게임**

으로 유지한다.

---

## 3. 직업 선택의 의미 유지

4개 직업은 단순히 스탯 숫자만 다른 캐릭터가 아니다.

각 직업은 특정 위기에서 한 번씩 강력한 이점을 갖도록 설계되어 있다.

```text
AQUARIST
→ 물리 파괴 / 완력

DIVER
→ 침수 / 감전 / 수중 위기

VET
→ 분석 / 공포 저항

SECURITY
→ 시설 / 방재 / 잠금 해제
```

이러한 직업별 특권은 밸런스 오류가 아니라 의도된 설계다.

플레이어가 게임을 다시 시작했을 때

> "이번에는 다른 직업으로 해보면 이 상황을 다르게 넘길 수 있겠다."

라는 생각이 들게 만드는 것이 목표다.

---

## 4. 텍스트를 읽는 게임이 아니라 장면을 체험하는 게임

시나리오 원문은 분위기를 위해 비교적 자세하게 작성되어 있지만,
실제 플레이 화면에서 긴 설명을 그대로 보여주지는 않는다.

기본 원칙:

```text
구역 설명: 2줄
조사 결과: 약 2줄
위기 장면: 필요하면 조금 길게
엔딩: 충분한 연출 허용
```

텍스트 양을 줄이는 대신

- 장면 이미지
- 화면 플래시
- 암전
- 사운드
- 주사위
- HP / SAN 변화
- 장비 이미지

등 프론트엔드 인터랙션을 이용해 상황을 전달한다.

즉,

**읽어서 이해하는 게임보다 눌러보고 반응을 보면서 이해하는 게임**

을 목표로 한다.

---

## 5. 포트폴리오로서의 목적

이 프로젝트는 상용 게임을 만드는 것이 아니라
프론트엔드 포트폴리오용 인터랙티브 프로젝트다.

따라서 콘텐츠의 양을 무작정 늘리는 것보다 다음 요소를 보여주는 것이 중요하다.

- React 상태 변화에 따른 화면 전환
- 조건부 렌더링
- 직업 / 아이템 / 플래그 기반 분기
- D20 판정 시스템
- 인벤토리 상태 관리
- 모달 시퀀스
- CSS 애니메이션과 화면 효과
- 사운드 인터랙션
- 멀티 엔딩
- 엔딩 컬렉션
- 동일 콘텐츠의 재플레이 가치

최종적으로는 짧은 플레이 시간 안에서도

> **"이 사람이 단순히 화면을 만든 것이 아니라,**
> **인터랙션과 콘텐츠 흐름을 설계하고 React로 구현했다."**

는 점이 드러나는 것을 목표로 한다.

---

# 최종 목표

플레이어가 게임을 끝냈을 때

```text
예쁜 화면을 봤다
```

보다

```text
내가 고른 직업과
내가 조사한 장소와
내가 챙긴 장비 때문에
이렇게 살아남았다
```

라는 인상을 받게 만드는 것.

그리고 포트폴리오를 보는 사람에게는

**시나리오, UI, 상태 관리, 조건 분기, 시각 연출이 하나의 사용자 경험으로 연결되어 있다는 것을 보여주는 것**

이 이번 수정 작업의 최종 목적이다.

---
# 아쿠아리움: 심해의 균열 — 남은 수정사항 인수인계

## 작업 기준

수정 우선순위는 다음과 같습니다.

1. 최종 시나리오 마스터 명세서
2. 최초 `app.jsx` 프로토타입
3. 현재 React 프로젝트

중요: 시나리오를 정리한다는 이유로 설명 모달, 조사 결과 모달, 장비 획득 모달을 다시 합치지 않습니다.

---

## 현재까지 복구된 사항

### 모달 역할 분리
- `SCENE`: 구역 진입 / 위기 연출 / 장면 이미지
- `RESULT`: 일반 조사 결과 / 텍스트
- `REWARD`: 장비 획득 / 장비 이미지만 표시

장비 획득 시 현재 장면 이미지와 장비 이미지가 같이 뜨던 문제는 분리했습니다.

### 스테이지 진입 설명 모달 복구
현재 기본 흐름:
```text
컨디션 주사위
→ STAGE 1 진입 설명
→ 조사
→ 위기
→ 다음 스테이지
→ 해당 스테이지 진입 설명
```

### STAGE 1 다이버 특권 복구
다이버 시작 장비:
- `LINE_CUTTER`
- `WETSUIT`

STAGE 1 누전 위기:
```text
WETSUIT 보유
→ DEX DC 11
→ 성공 HP -6 후 STAGE 2
→ 실패 HP -10 후 STAGE 2
```

실패해도 BAD END 1로 가지 않도록 복구됨.

### STAGE 1 누전 플래시 연출
누전 폭발 시:
```text
백색 플래시
→ 재점멸
→ 누전 폭발 모달
```

현재 표시 동작 확인 완료.

---

## 화면 텍스트 작성 원칙

### 구역 진입 설명
2줄 최적화.

권장 구조:
```text
1줄: 지금 보이는 장면
2줄: 위험 / 불길한 징후 / 다음 행동 암시
```

### 조사 결과
가능하면 2줄 내외.

### 위기 모달
스테이지 클라이맥스이므로 구역 설명보다 조금 길어도 됨.

### 엔딩
감정적 보상 구간이므로 길게 사용 가능.

---

# STAGE 1

현재 목표 흐름:
```text
해파리 터널 진입
→ 조사 3회
→ AP 0
→ 누전 폭발
→ 백색 플래시
→ 위기 설명
→ 생존 판정
→ STAGE 2
```

생존:
- `hasRubberBoots`
- `isGateUnlocked`

다이버:
```text
WETSUIT
→ DEX DC 11
→ 성공 HP -6
→ 실패 HP -10
→ 둘 다 STAGE 2
```

그 외 생존 수단 없음:
```text
BAD END 1
```

---

# STAGE 2

## 진입 설명 권장
```js
sub: `폭발의 충격 직후, 해수 배관이 파열되며 거대한 물줄기가 통로를 집어삼켰다.
무릎을 넘긴 물 너머로 40cm 강철 수밀문이 굉음을 내며 내려오기 시작한다.`,
```

## 생존 판정
빠루 보유:
```text
hasCrowbar
→ STR DC 10
```

빠루 없음 + 수압 감소 성공:
```text
isPressureReduced
→ DEX DC 12
```

둘 다 없음:
```text
BAD END 2
```

## AP 0 연출
```text
closingBg
→ "철컥, 쿵—!"
→ 격벽 폐쇄 설명 모달
→ 다음
→ 실제 판정
```

---

# STAGE 3

## 진입 설명 권장
```js
sub: `방열문이 닫히자 영하 35도의 백색 냉풍이 젖은 몸을 덮쳤다.
래치는 순식간에 얼어붙었고, 이 문을 녹이지 못하면 여기서 끝이다.`,
```

## 생존 판정
토치 보유:
```text
hasHeatingTorch
→ DEX DC 8
```

토치 없음 + 냉각기 정지:
```text
isChillerOff
→ STR DC 13
```

둘 다 없음:
```text
BAD END 3
```

## AP 0 위기 연출
```text
동결 한계 설명
→ 다음
→ 토치 / 냉각기 여부 확인
→ 판정
```

---

# 직업별 특권

## AQUARIST
역할:
```text
물리 파괴 / STR 특화
```

주요 적용:
- STR 계열 판정 +2
- 수밀문
- 냉동고 강제 파쇄
- 최종 돔 파쇄 보조

시작 장비 `RUBBER_BOOTS`는 의도된 직업 특권 성격이 있으므로 임의 삭제하지 않음.

## DIVER
역할:
```text
침수 / 누전 / 수중 위기 특화
```

STAGE 1:
```text
WETSUIT
→ 누전 즉사 대신 DEX DC 11
```

STAGE 4:
```text
c4_3 흡입 그릴
+ LINE_CUTTER
→ STR DC 13 대신 DEX DC 9
```

## VET
역할:
```text
공포 저항 / 분석 특화
```

STAGE 3 냉각기 제어반:
```text
INT DC 12 → DC 9
```

권장 구현:
```js
if (
  state.character.key === 'VET' &&
  id === 'c3_3'
) {
  dc = 9;
}
```

## SECURITY
역할:
```text
시설 / 방재 / 잠금 해제 특화
```

STAGE 4 방재 장비 보관함:
```text
DEX DC 12 → DC 8
```

권장 구현:
```js
if (
  state.character.key === 'SECURITY' &&
  id === 'c4_4'
) {
  dc = 8;
}
```

---

# STAGE 4

## 진입 설명 권장
```js
sub: `허리까지 차오른 해수와 뜨거운 증기 사이로 거대한 배수 펌프가 굉음을 토해냈다.
붉은 경고등 아래, 00:30 해수 방류 프로토콜까지 남은 시간은 10분.`,
```

## 조사 데이터
### c4_1 중앙 전력 제어반
- `MASTER_KEYCARD`

### c4_2 고압 증기 바이패스 밸브
- HP -4
- SAN -3

### c4_3 바닥 거대 흡입 그릴
일반:
```text
STR DC 13
실패 HP -7
```

다이버 + 라인 커터:
```text
DEX DC 9
```

`handleExamine()`에서 공통 판정보다 먼저 분기.

### c4_4 방재 장비 보관함
일반:
```text
DEX DC 12
```

보안요원:
```text
DEX DC 8
```

획득:
```text
OXYGEN_MASK
```

### c4_5 수면 위 배선 트레이
- HP -5

### c4_6 메인 펌프 비상 제동 레버
```text
INT DC 12
성공 → isVortexStopped
```

보안요원 INT 특권 적용.

## STAGE 4 AP 0
최종 시나리오에는 별도 즉사 결단이 명시되어 있지 않음.

현재:
```text
AP 0
→ STAGE 5
```

유지.

---

# STAGE 5 — 최종 탈출전

기존의

장비 개수 계산
→ DC 결정
→ 주사위 1회
→ 엔딩

구조를 사용하지 않는다.

최종장은 반드시 3단계 연속 탈출로 진행한다.

---

## STEP 1 — 압력 해제

돔 내부에 쌓인 수압 때문에 그대로 유리를 깨면
플레이어가 분출수에 휩쓸릴 위험이 있다.

목표:
압력 배출구를 확보한다.

주요 장비:
- HEATING_TORCH

판정:
- INT 계열

장비가 있으면 DC 감소.
장비가 없어도 시도 가능하지만 높은 DC 및 실패 패널티 적용.

직업 보정:
- SECURITY / VET 계열 분석 능력 활용 가능

실패:
- 즉시 엔딩 금지
- HP 감소
- finalFailure +1
- 이후 최종 탈출 난이도 증가

Natural 1:
- 큰 피해
- finalFailure +2

---

## STEP 2 — 돔 파쇄

압력을 낮춘 뒤 강화 아크릴 돔을 실제로 파쇄한다.

주요 장비:
- CROWBAR

AQUARIST:
- STR 보너스
- 빠루가 없을 경우 TONGS를 대체 도구로 사용할 수 있음

판정:
- STR

성공:
→ 돔 균열 및 파쇄 연출
→ breakBg 전환

실패:
- HP 감소
- finalFailure +1
- 재시도 없이 STEP 3으로 진행
- 깨진 틈이 작아져 최종 탈출 판정 불리

Natural 1:
- 어깨 부상 등 큰 피해
- finalFailure +2

---

## STEP 3 — 분출수 탈출

돔이 무너지는 순간 대량의 해수가 지상으로 분출한다.

이 단계의 목표는

'유리를 깨는 것'

이 아니라

'분출수에 휩쓸리면서도 살아서 지상까지 도달하는 것'

이다.

주요 장비:
- OXYGEN_MASK

판정:
- DEX

DIVER:
- 수중 탈출 특권 적용

산소마스크:
- DC 감소
- 실패 피해 감소

Natural 1:
→ 최종 실패 누적 +2; 수직 탈출 단계의 Natural 1은 BAD END 4

일반 실패:
→ 누적 finalFailure와 장비 상태를 기준으로 엔딩 판정

성공:
→ 최종 엔딩 판정

---

# 엔딩 판정

TRUE END
- STEP 1~3 모두 성공
- HEATING_TORCH
- CROWBAR 또는 AQUARIST 대체 수단
- OXYGEN_MASK
- finalFailure === 0

GOOD END
- 탈출 성공
- finalFailure 0~1
- 핵심 장비 일부 부족 또는 경미한 부상

NORMAL END
- 탈출 성공
- finalFailure 2 이상
- 장비 부족 / 큰 부상 / 위험한 강행 돌파

BAD END 4
- 수직 탈출 단계에서 Natural 1
- 최종 탈출 도중 HP 또는 SAN 소진
- finalFailure가 3 이상

---

# 중요한 설계 원칙

아이템 = 자동 성공이 아니다.

아이템은:
- DC 감소
- 피해 감소
- 특수 선택지 개방

역할을 한다.

따라서 좋은 장비를 얻어도
플레이어는 최종 탈출에서 직접 3번의 결정을 수행한다.

최종전 UI 역시

[1. 압력 해제]
↓
[2. 돔 파쇄]
↓
[3. 수직 탈출]

의 진행도를 명확히 보여준다.

### 반드시 수정해야 하는 현재 문제
최종 엔딩은 누적 실패 횟수와 준비한 장비를 기준으로 판정한다.

---

# 엔딩 문구 복구

최종적으로 다음 엔딩 텍스트를 시나리오 기준으로 정리:

- BAD END 1
- BAD END 2
- BAD END 3
- BAD END 4
- NORMAL END
- GOOD END
- TRUE END

UI 길이에 맞게 다듬되 핵심 사건과 결말은 유지.

---

# 아이템 관련 남은 정리

## STAGE 1 직원 전용 보관함
원본:
```text
LINE_CUTTER
+
소형 방수 펜라이트
```

현재 단일 reward 구조 때문에 `LINE_CUTTER`만 획득.

추후 다중 보상 지원 여부 결정.

권장 형태:
```js
rewards: ['LINE_CUTTER', 'PENLIGHT']
```

단, 인벤토리 5칸 제한 / 중복 처리 확인 필요.

## STAGE 2 케이블 타이
원본:
```text
공업용 고장력 케이블 타이
```

현재 텍스트상 획득하지만 실제 `ITEM_DB`에는 없음.

후속 판정에 쓰지 않는다면:
- 분위기용 획득물로 유지
- 또는 실제 아이템화 여부 결정

---

# handleExamine() 최종 권장 구조

```js
if (point.checkStat) {
  const bonus =
    (
      state.character.key === 'AQUARIST' &&
      point.checkStat === 'STR'
    ) ||
    (
      state.character.key === 'SECURITY' &&
      point.checkStat === 'INT'
    )
      ? 2
      : 0;

  let dc = point.dc;

  if (
    state.character.key === 'VET' &&
    id === 'c3_3'
  ) {
    dc = 9;
  }

  if (
    state.character.key === 'SECURITY' &&
    id === 'c4_4'
  ) {
    dc = 8;
  }

  if (
    id === 'c4_3' &&
    state.character.key === 'DIVER' &&
    state.inventory.some(item => item.id === 'LINE_CUTTER')
  ) {
    triggerD20(
      point.name,
      'DEX',
      9,
      0,
      () => resolve(true),
      () => resolve(false)
    );

    return;
  }

  triggerD20(
    point.name,
    point.checkStat,
    dc,
    bonus,
    () => resolve(true),
    () => resolve(false)
  );

  return;
}

resolve(true);
```

---

# 절대 하지 말 것

## 모달 통합 금지
다시 아래처럼 합치지 않음:
```text
구역 설명
+
조사 결과
+
장면 이미지
+
장비 이미지
```

## 스테이지 전환 자동 계산 남발 금지
과거 중복 전환으로:
```text
STAGE 1 → STAGE 3
```
문제 발생.

가능하면 목적지를 명시:
```js
advanceStage('STAGE_2_BULKHEAD');
advanceStage('STAGE_3_FREEZER');
advanceStage('STAGE_4_PUMP');
advanceStage('STAGE_5_DOME');
```

## 직업 특권 제거 금지
직업별 치트성 강점은 의도된 설계.

## 장면 설명 장문화 금지
실제 플레이 UI에서는 2줄 우선.

---

# 앞으로의 권장 작업 순서

## STEP 1 — STAGE 4 검증
- 다이버 `c4_3` DEX 9
- 수의사 `c3_3` DC 9
- 보안요원 `c4_4` DC 8
- c4_2 SAN -3
- STAGE 4 → STAGE 5 정상 진입

## STEP 2 — STAGE 5 엔딩 매트릭스 완전 복구
```text
3종 성공 TRUE / 실패 GOOD
2종 성공 GOOD / 실패 NORMAL
1종 이하 성공 NORMAL / 실패 BAD
Natural 1 BAD
```

## STEP 3 — 엔딩 연출 / 문구 정리

## STEP 4 — 아이템 데이터 정리
- STAGE 1 2개 동시 획득
- 케이블 타이 처리
- 인벤토리 5칸 / 중복 정책

## STEP 5 — 전체 회귀 테스트
### AQUARIST
- STR 특권
- STAGE 1 직업 특권
- 최종 파쇄

### DIVER
- STAGE 1 WETSUIT
- STAGE 4 LINE_CUTTER

### VET
- STAGE 3 INT DC 9
- 공포 피해 감소

### SECURITY
- INT +2
- STAGE 4 방재함 DC 8

## STEP 6 — 엔딩 7종 컬렉션 확인
- BAD 1
- BAD 2
- BAD 3
- BAD 4
- NORMAL
- GOOD
- TRUE

---

# 가장 중요한 다음 작업

> **`useAquariumGame.js`의 `handleStage5Break()`를 최종 시나리오 기준으로 수정**

다른 파일은 건드리지 않고,
STAGE 5의 성공/실패 엔딩 매트릭스부터 정확히 복구하는 것을 권장합니다.
